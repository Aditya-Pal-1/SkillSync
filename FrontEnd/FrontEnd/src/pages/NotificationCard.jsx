import { Bell, Check, X } from "lucide-react";
const NotificationCard=({notification})=>{ const skillName=notification.skill?.name||"Skill"; const date=new Date(notification.updatedAt); const map={pending:[`Your ${skillName} booking is waiting for confirmation`,Bell,"pending"],confirmed:[`Your ${skillName} booking has been confirmed`,Check,"confirmed"],cancelled:[`Your ${skillName} booking was cancelled`,X,"cancelled"],completed:[`Your ${skillName} booking is completed`,Check,"completed"]}; const [title,Icon,type]=map[notification.status]||[`Your ${skillName} booking was updated`,Bell,"confirmed"]; return <div className="booking-row"><div style={{display:"flex",alignItems:"center",gap:12}}><div className="empty-icon" style={{width:40,height:40,margin:0}}><Icon size={18}/></div><div className="booking-main"><div className="booking-title">{title}</div><div className="booking-meta"><span>{date.toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"})}</span></div></div></div><span className={`status ${type}`}>{notification.status}</span></div>; };
export default NotificationCard;
